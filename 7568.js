const fs = require("fs");
let input = fs.readFileSync("./dev/stdin.txt").toString().trim().split("\n");

input.shift();

for (let i = 0; i < input.length; i++) {
  // input[]
}

let a = 2666;
console.log(a.toString().includes("666"));
